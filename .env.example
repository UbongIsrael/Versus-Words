# Copy into the matching app, or set the same keys in the host dashboard.
#   API:        apps/api/.env.example  →  apps/api/.env.local  or Railway/Render env
#   Frontend:   apps/web/.env.example  →  apps/web/.env.local  or Vercel env (build time)
#
# Frontend needs VITE_API_URL when it is not on the same origin as the API.
